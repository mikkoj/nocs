namespace Nocs.Models
{
    public enum StatusType
    {
        Authorize,
        Save,
        Sync,
        Error,
        Retrieve,
        Info,
        ContentUpdateError,
        Reset
    }
}
